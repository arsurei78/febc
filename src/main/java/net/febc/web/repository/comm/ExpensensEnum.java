package net.febc.web.repository.comm;

import com.sun.mail.iap.Response;

public enum ExpensensEnum {

    // 교사 사례비(TS), 지방세(LOT), 소득세(INT), 선물(GIFT), 간식비(SNACK), 악보제본비(SHEET), DUES(총회비), DONA(후원금), BINT(은행이자)
    TS, LOT, INT, GIFT, SNACK, SHEET, ORTHER, DUES, DONA, BINT;

}
